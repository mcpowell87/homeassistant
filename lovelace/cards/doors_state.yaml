type: custom:vertical-stack-in-card
title: 
cards:
  - entity: sensor.open_door_count
    scale: 15px
    name: >
      [[[
        if (entity.state == 1) 
          return "1 door open.";
        else if (entity.state > 1)
          return entity.state + " doors open.";
        else
          return "All doors closed."
      ]]]
    type: 'custom:button-card'