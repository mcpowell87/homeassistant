# Main /lovelace dashboard is YAML mode, served from ui-lovelace.yaml
# (a symlink to lovelace/dashboards/overview-yaml.yaml). UI-created
# dashboards (Admin, Map) live in .storage and stay UI-editable.
lovelace:
  mode: yaml
