---
recorder:
  db_url: !secret db_url
  purge_keep_days: 14
  auto_purge: true
  auto_repack: true
  commit_interval: 30
  exclude:
    entity_globs:
      - sensor.weather_*
      - sensor.sun*
      - weather.*
    entities:
      - sun.sun
      - sensor.date
